import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App';    // NOTE: gets app.js inside the compnent folder
ReactDOM.render(
  <App/>, document.querySelector('#root')
);
